services:
  shuttled:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "443:443/tcp"
      - "443:443/udp"
    volumes:
      - ./config:/etc/shuttle:ro
      - certs:/etc/letsencrypt:ro
    restart: unless-stopped

volumes:
  certs:
